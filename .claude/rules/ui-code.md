---
paths:
  - "src/ui/**"
---

# UI 代码规则

- UI 绝不得拥有或直接修改游戏状态——仅负责显示，通过命令/事件请求变更
- 所有 UI 文本必须经过本地化系统——禁止硬编码面向用户的字符串
- 所有可交互元素必须同时支持键盘/鼠标和手柄输入
- 所有动画必须可跳过，并尊重用户的动效/无障碍偏好设置
- UI 音效通过音频事件系统触发，不得直接调用
- UI 绝不得阻塞游戏主线程
- 可缩放文本和色盲模式是强制要求，而非可选功能
- 在最小和最大支持分辨率下测试所有界面
