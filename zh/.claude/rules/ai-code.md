---
paths:
  - "src/ai/**"
---

# AI 代码规则

- AI 更新预算：每帧最多 2ms——需通过性能剖析验证
- 所有 AI 参数必须可从数据文件调整（行为树权重、感知范围、计时器等）
- AI 必须可调试：为所有 AI 状态实现可视化钩子（路径、感知锥、决策树）
- AI 应预告意图——玩家需要时间读取并作出反应
- 优先采用效用驱动或行为树方案，而非硬编码的 if/else 链
- 群体 AI 必须支持从数据配置阵型、侧翼和角色分配
- 所有 AI 状态机必须记录状态转换日志，以便调试
- 绝不信任来自网络的 AI 输入，须经过验证后方可使用
